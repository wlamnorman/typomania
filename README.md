# Hobby project WPM test with a cursed twist
![Demo](demo.gif)

# Build
Build the binary using `make install` at the repository root.

# Run
After building, simply type "typomania" to start the CLI. Use the `--help` flag to get CLI command descriptions.
